self.$config = {
  // true 将会默认使用 `landing.html`
  landing: true,
  // 当然 markdown 文件也可以
  landing: 'landing.md',
  nav: [
    // 首页
    {title: 'Home', path: '/home'},
    // 中文文档
    {title: '中文文档', path: '/language/chinese'}
  ]
}
